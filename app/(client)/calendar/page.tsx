import Calendar from "@/components/Calendar";
import React from "react";

const page = () => {
  return (
    <section className="w-full h-full mt-[100px] min-h-screen flex justify-center items-center">
      <Calendar />
    </section>
  );
};

export default page;
